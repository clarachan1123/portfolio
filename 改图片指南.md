# 改图片指南

> **一句话规则：只改宽度，高度让它自己算。**
> 记住这一句，图片就永远不会变形。

---

## 一、先把两个词分清楚

「图片变形」其实是两种完全不同的毛病，混在一起说就找不到原因：

| | 长什么样 | 真正的原因 |
|---|---|---|
| **拉伸变形** | 图被压扁或拉长——人脸变胖、圆形变椭圆 | **宽高比**被改了 |
| **裁切** | 图的比例正常，但上下或左右被切掉一块 | 容器只显示了一部分 |

**宽高比** = 宽 ÷ 高。

一张 1000 × 1308 的图，宽高比是 `1000 ÷ 1308 = 0.765`。
只要新尺寸算出来的比值还是 0.765，这张图就**不可能**变形。

一旦有人把尺寸写死成「宽 800、高 500」（比值 1.6），图立刻被拉扁——
因为 0.765 变成了 1.6。

---

## 二、AI 为什么老把图弄坏 —— 三种典型做法

1. **同时写死宽度和高度**（`width:800px; height:500px`）→ 拉伸变形
2. **用「填满容器」的方式**（CSS 里的 `object-fit: cover`）→ 比例没变，但**内容被切掉**
3. **用图片工具时选「拉伸到指定尺寸」**，而不是「等比缩放到指定宽度」→ 拉伸变形

**你之前遇到的是第 2 种。**

首页原来把一张竖版截图硬塞进一个横条框里，`cover` 的规则是「填满框、多的切掉」，
结果切掉了原图 **52%** 的高度。比例是对的，但图确实"坏了"。

> 这个我已经修好了。现在全站 22 张图逐张量过，**没有一张被裁切或拉伸**，
> 比例偏差都在 0.7% 以内（多数是 0.0%）。

---

## 三、换图：用「图片工具.html」

网站根目录里有个 `图片工具.html`，双击就能用（用浏览器打开，不用装东西）。

**第 1 步｜选位置**

下拉框里列了网站所有图片，每一项都写着「文件名 → 用在哪」。
比如 `首页项目01缩略图 · Gloss 案例页最后一节 → gloss-reader.webp`。

**第 2 步｜把图拖进去**

拖进虚线框，或者点一下选文件。

**第 3 步｜下载，然后放进 `assets\img\`**

1. 点「下载」，文件会进你的「下载」文件夹
2. 把它**移动**到网站的 `assets\img\` 文件夹里
3. 如果提示「已有同名文件」，选**替换**
4. 回到浏览器，按 **Ctrl + F5**（强制刷新）

**全程不用改任何代码。** 文件名是工具自动填好的——网站就是靠文件名找到图片的。

这个工具做了两条硬保证：

- **永远只等比缩放**：实测 6 种情况，比例偏差全部 `0.0000%`
- **图比需要的还小就不放大**（放大会糊），并且明确告诉你「这张图偏小」

---

## 四、不想用这个工具，自己拿别的软件做也行

关键只有一句：**只填宽度，高度留空或者选「自动」。**

| 工具 | 怎么做 |
|---|---|
| Photoshop | 图像 → 图像大小 → **勾上「约束比例」** → 只改宽度那一栏 |
| 美图秀秀 / 在线压缩站 | 找「**等比缩放**」，不要用「裁剪到指定尺寸」 |
| 手机相册 | 选「调整尺寸」时留意有没有「保持比例」选项 |
| Figma | 选中图片，拖角落的圆点时按住 `Shift` |

**自查方法**：算一下 `新宽度 ÷ 原宽度`，它应该等于 `新高度 ÷ 原高度`。

> 例：1000 × 1308 的图，想缩到宽 800。
> 高度应该是 `1308 × 800 ÷ 1000 = 1046`。
> 如果算出来不是 1046，说明被拉伸了。

---

## 五、网站里每张图对应哪里

| 文件名 | 用在哪 | 建议宽度 |
|---|---|---|
| `gloss-shelf.webp` | 首页 · 封面大图 | 1200 px |
| `gloss-reader.webp` | 首页项目 01 缩略图 · Gloss 案例页最后一节 | 1000 px |
| `gloss-rewrite.png` | Gloss 案例页 §06「规则」 | 685 px |
| `gloss-shelf-empty.png` | Gloss 案例页最后一节 · 书架空态 | 1600 px |
| `gloss-spec-cover.webp` | Gloss §07 横滑第 1 张 · 规格总览 | 800 px |
| `gloss-spec-shelf.webp` | Gloss §07 横滑第 2 张 · 首页书架 | 800 px |
| `gloss-spec-intake.webp` | Gloss §07 横滑第 3 张 · 录入校验 | 800 px |
| `gloss-spec-reader.webp` | Gloss §07 横滑第 4 张 · 阅读器三栏 | 800 px |
| `gloss-spec-states.webp` | Gloss §07 横滑第 5 张 · 点句六态 | 800 px |
| `gloss-spec-hear.webp` | Gloss §07 横滑第 6 张 · 听不懂 | 800 px |
| `gloss-spec-edit.webp` | Gloss §07 横滑第 7 张 · 编辑白话 | 800 px |
| `gloss-spec-export.webp` | Gloss §07 横滑第 8 张 · 设置与导出 | 800 px |
| `gloss-spec-audit.webp` | Gloss §07 横滑第 9 张 · 审计清单 | 800 px |
| `gloss-spec-principles-a.webp` | Gloss §07 原则卡（左半） | 1100 px |
| `gloss-spec-principles-b.webp` | Gloss §07 原则卡（右半） | 1100 px |
| `feynman-home.webp` | Feynman 案例页顶部 · 首页 | 682 px |
| `feynman-session.webp` | Feynman 案例页顶部 · 讲述演练 | 682 px |
| `feynman-map.webp` | 首页项目 02 缩略图 · Feynman 案例页 §04 | 760 px |
| `feynman-quiz.webp` | Feynman 案例页 §02 · 三级提示 | 760 px |
| `feynman-reangle.webp` | Feynman 案例页 §02 · 换个切入点 | 760 px |

**「建议宽度」怎么理解**：填这个宽度，图在普通屏幕和手机上都清楚。
填得比它小，图会稍微糊一点；填得比它大，网页会变慢，但不会变形。

---

## 六、五条铁律

1. **文件名不要改。** 网站靠文件名找图，改了名就找不到了。
2. **不要放大。** 原图多宽就多宽。小图放大只会更糊，不会更清楚。
3. **优先存 webp。** 同样清晰度下比 png / jpg 小一大截。
4. **宽度别超过 1600 px。** 再大网页打开就慢了。
5. **改完按 Ctrl + F5。** 普通 F5 有时会读缓存，看不到新图。

---

## 七、出问题对照表

| 现象 | 原因 | 怎么办 |
|---|---|---|
| 换完没变化 | 浏览器读了缓存 | 按 **Ctrl + F5** |
| 图很糊 | 原图太小，被放大了 | 换一张更大的原图 |
| 图被切掉一块 | 这个位置的比例和你的图不匹配 | **告诉我**，我改样式，不用你自己动 |
| 网页打开变慢 | 图太大 | 用图片工具重做一遍 |
| 图整个不见了 | 文件名被改了 | 文件名必须和原来一模一样 |

---

## 八、如果还是不想弄

**把原图直接丢给我，告诉我放哪就行。**

比如：「这张换成首页封面」「这张加到 Feynman 那页」——我来处理尺寸和格式。
你只要确认效果。

---

## 附：网站文件在哪

```
C:\Users\23750\OneDrive\桌面\作品集\
├── index.html          首页
├── gloss.html          Gloss 案例页
├── feynman.html        费曼案例页
├── about.html          关于页
├── 图片工具.html        ← 换图用这个
├── 改文案指南.md        改文字看这个
├── 改图片指南.md        本文件
├── assets\
│   ├── style.css       样式（配色、字体、间距都在这）
│   └── img\            ← 所有图片放这里
└── 素材\               原始素材，不参与发布
```
