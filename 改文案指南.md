# 改文案指南

这个网站是纯 HTML 写的，没有任何构建步骤——**改字就是改文件里的字，存盘、刷新，立刻生效**。

你不需要懂代码，只需要守住下面三条铁律。

---

## 一、三步改字

**第 1 步｜打开文件**

四个页面对应四个文件：

| 文件 | 是哪个页面 |
|---|---|
| `index.html` | 首页 |
| `gloss.html` | Gloss 案例页 |
| `feynman.html` | 费曼案例页 |
| `about.html` | 关于页 |

右键文件 → 打开方式 → 记事本。或者装一个 **VS Code**（免费，搜 `Visual Studio Code`），有高亮和全局搜索，改起来舒服得多。

**第 2 步｜找到那句话**

打开文件后，最上面有一段 `✏️ 改文案指南` 的注释，里面列了这个页面所有能改的位置，每条都给了**搜索关键词**。

按 `Ctrl + F`，输入关键词，回车，就跳到那句话了。

**第 3 步｜改字，存盘，刷新**

只改中文。存盘（`Ctrl + S`）。回到浏览器按 `F5`。

---

## 二、三条铁律

### 铁律 1：只改中文，别碰符号

```
改之前：  <h1>两个我自己定义、<br>自己验收的 AI 产品。</h1>
                ↑ 改这里                              ↑ 这些别动

改之后：  <h1>我把读不懂的句子，<br>变成读得懂的。</h1>
```

`<h1>` `</h1>` `<br>` 这些叫标签，它们告诉浏览器"这是标题""这里换行"。**删掉标签，文字会跑版；删掉闭合标签，后面的内容会跟着变形。**

### 铁律 2：不要在正文里直接写 `<` `>` `&`

这三个符号在 HTML 里有特殊含义。真要用，写成这样：

| 你想写 | 要写成 |
|---|---|
| `<` | `&lt;` |
| `>` | `&gt;` |
| `&` | `&amp;` |

中文引号 `「」` `""` `（）` 都是安全的，随便用。

### 铁律 3：绝对不要用 Word 打开这些文件

**这是最容易出事的一条。** Word 会按自己的格式重写整个文件，标签会被破坏，页面会直接烂掉。

要编辑，只用：记事本、VS Code、Sublime Text、Notepad++。**不要用 Word、不要用 WPS 文字。**

---

## 三、常见改法

### 想加一个新项目（比如第三个产品）

打开 `index.html`，搜 `02 费曼`，会看到一整块：

```html
<!-- ===================== 02 费曼 ===================== -->
<article class="entry">
  ...
</article>
```

把这一整块（从 `<!--` 到 `</article>`）复制一份，粘在它下面，然后把里面的文字改成新项目的。**记得把 `<div class="entry-no">02</div>` 改成 `03`。**

如果新项目也需要一个独立的深度案例页，那就复制 `feynman.html` 整个文件，改个名字（比如 `project3.html`），再把里面内容换掉——同时别忘了在四个页面的导航栏里加一条链接。

### 想换配色

打开 `assets/style.css`，文件最上面有一块：

```css
:root {
  --paper:      #F8F6F0;   /* 页面底色 */
  --ink:        #191815;   /* 正文字色 */
  --accent:     #8A6A32;   /* 强调色（小标签、链接下划线） */
  ...
}
```

改这几个十六进制色值就行。改完存盘刷新，四个页面一起变。

### 想换字体

同一个文件里的：

```css
--serif: ...    /* 标题用的衬线字体 */
--sans:  ...    /* 正文用的字体 */
--mono:  ...    /* 小标签、数字用的等宽字体 */
```

字体名要用**英文**，且必须是访客电脑上装了的字体（或者用系统自带的中文字体栈）。

### 想改图片

把新图片放进 `assets/img/` 文件夹，然后在 HTML 里搜 `src="assets/img/`，把文件名换掉即可。

建议图片宽度不超过 1600px，单张控制在 500KB 以内，否则网页打开会变慢。

---

## 四、改坏了怎么办

我在同一层目录留了一份原始备份：

```
C:\Users\23750\WorkBuddy AI\2026-09-21-03-20-13\portfolio-原始备份-20260921\
```

里面是建站当天最原始的版本。如果改到页面全乱了：

1. 打开备份文件夹，找到对应的文件（比如 `gloss.html`）
2. 复制它
3. 回到 `portfolio` 文件夹，粘贴、覆盖

就恢复成原样了。

**或者直接找我**，告诉我哪个页面不对，我帮你还原。

---

## 五、改完文案之后

如果你改动了**事实性的内容**——比如实习时间、项目数据、联系方式——记得同步改一下 `基础简历.md`（在 `2026-09-19-16-45-00` 文件夹里）。

现在简历和作品集之间已经有几处对不上了（我在建站时核对出来过），两边保持一致，面试时才不会自相矛盾。

---

## 六、还没做的事

- **联系方式**：页脚现在写的是「邮箱与电话 · 见简历」，没有放真实手机号（公开网页上放手机号容易被爬）。要放邮箱的话，告诉我。
- **蜂阁实习**：关于页的时间线里有这一条，但你的简历文件里没有这段，需要你确认。
- **部署上线**：目前只在本地，还没有公开链接。
