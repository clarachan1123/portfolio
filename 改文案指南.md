# 改文案指南

这个网站是纯 HTML 写的，没有任何构建步骤——**改字就是改文件里的字，存盘、刷新，立刻生效**。

你不需要懂代码，只需要守住下面三条铁律。

---

## 一、三步改字

**第 1 步｜打开文件**

三个页面对应三个文件：

| 文件 | 是哪个页面 |
|---|---|
| `index.html` | 首页（封面 + 关于 + 时间线 + 教育与工具） |
| `gloss.html` | Gloss 案例页（10 章） |
| `feynman.html` | 费曼案例页（6 章） |

还有一个 `about.html`，它现在只做一个跳转——因为新版把「关于」并进了首页，这一页是为了让老链接不失效才留着的。**里面的内容不用改，也不用维护。**

右键文件 → 打开方式 → 记事本。或者装一个 **VS Code**（免费，搜 `Visual Studio Code`），有高亮和全局搜索，改起来舒服得多。

**第 2 步｜找到那句话**

打开文件后，最上面有一段 `✏️ 改文案指南` 的注释，里面列了这个页面所有能改的位置，每条都给了**搜索关键词**。

按 `Ctrl + F`，输入关键词，回车，就跳到那句话了。

**第 3 步｜改字，存盘，刷新**

只改中文。存盘（`Ctrl + S`）。回到浏览器按 `F5`。

> **本地怎么预览**：直接双击 `index.html`，浏览器就能打开。整个网站不用装任何东西。

---

## 二、三条铁律

### 铁律 1：只改中文，别碰符号

```
改之前：  <h1>两个我自己定义、<br>自己验收的 AI 产品。</h1>
                ↑ 改这里                              ↑ 这些别动

改之后：  <h1>我把读不懂的句子，<br>变成读得懂的。</h1>
```

`<h1>` `</h1>` `<br>` 这些叫标签，它们告诉浏览器"这是标题""这里换行"。**删掉标签，文字会跑版；删掉闭合标签，后面的内容会跟着变形。**

### 铁律 2：不要在正文里直接写 `<` `>` `&`

这三个符号在 HTML 里有特殊含义。真要用，写成这样：

| 你想写 | 要写成 |
|---|---|
| `<` | `&lt;` |
| `>` | `&gt;` |
| `&` | `&amp;` |

中文引号 `「」` `""` `（）` 都是安全的，随便用。

### 铁律 3：绝对不要用 Word 打开这些文件

**这是最容易出事的一条。** Word 会按自己的格式重写整个文件，标签会被破坏，页面会直接烂掉。

要编辑，只用：记事本、VS Code、Sublime Text、Notepad++。**不要用 Word、不要用 WPS 文字。**

---

## 三、常见改法

### 想加一个新项目（比如把 FamCal 加回首页）

首页的「项目索引」现在有**两张卡**：01 Gloss / 02 Feynman。
FamCal（蜂阁实习那个项目）目前只在下面的**时间线**里有一条，首页索引里没有它。

要把它加回索引，最省事的做法是**直接找我**——那张卡（卡片 + 图版导读 + 事项上下文模型表）
的代码原样留着，恢复起来是拼装，不用你手写。

想自己动手的话，步骤是：复制 02 那张卡那一整段（从 `<article class="entry">` 到 `</article>`），
粘在它下面，把里面的文字换成 FamCal 的、`entry-no` 改成 `03`，
再把区标题右边的 `01 — 02` 改成 `01 — 03`。

### 想换配色

打开 `assets/merged.css`，文件最上面有一块：

```css
:root {
  --night:   #17150F;   /* 深色区块的底（封面、页脚） */
  --paper:   #F4F1E9;   /* 浅色区块的底（正文） */
  --ink:     #1A1815;   /* 正文字色 */
  --accent:  #2E5B4B;   /* 强调色：只有墨绿这一个 */
  ...
}
```

改这几个十六进制色值就行。改完存盘刷新，三个页面一起变。

> ⚠️ **一页只允许一个强调色。** 这是这套设计的规矩——`--accent` 只用来标编号、分隔线、状态，
> 不做装饰。别再加第二个颜色，加了整站会立刻变花。

### 想换字体

同一个文件里的：

```css
--cjk:  ...    /* 中文（正文和标题都用它） */
--disp: ...    /* 拉丁字母和数字的大标题（Space Grotesk） */
--mono: ...    /* 小标签、数字用的等宽字体（IBM Plex Mono） */
```

字体名要用**英文**。中文那一栏**不要引外部字体文件**（体积太大），只写系统自带的中文字体栈。

### 想改图片

**去看另一份文件：`改图片指南.md`。**

里面讲清了「图片为什么会变形」以及怎么避免，还配了一个 `图片工具.html`：
把图拖进去、下载、覆盖同名文件就行，**不用改代码，也不可能把图弄变形**。

---

## 四、改坏了怎么办

**先别慌，也别手动改回去。** 这个文件夹是一个 git 仓库，每个版本都留着，随时能回退。

三种办法，从快到慢：

1. **只改错了一句话** → 按 `Ctrl + Z` 撤销，存盘。
2. **整个文件乱了** → 直接找我，说「把 `gloss.html` 恢复到上一个版本」，一句话的事。
3. **想自己看历史** → 在这个文件夹里右键 → Git Bash Here，敲 `git log --oneline` 看提交记录。

> 以前那份「原始备份」文件夹（在 `WorkBuddy AI\2026-09-21-03-20-13\`）**已经不存在了**，
> 别再照着老指南去找。现在的备份就是 git 历史 + GitHub 仓库。

---

## 五、改完文案之后

如果你改动了**事实性的内容**——比如实习时间、项目数据、联系方式——记得同步改一下
`基础简历.md`（在 `2026-09-19-16-45-00` 文件夹里）。

简历和作品集两边保持一致，面试时才不会自相矛盾。

---

## 六、现在的状态（2026-09-23）

- **线上地址**：`https://clara-lixing.kicp.fun`（花生壳 Drop，国内节点，手机流量实测很快）
- **代码仓库**：`github.com/clarachan1123/portfolio`，push 到 main 后 Vercel 会自动部署一份
  （备用地址 `clara-lixing.vercel.app`，但国内被 DNS 投毒，打不开）
- **还没放上去的**：FamCal 的详情页，以及首页索引里 FamCal 那张卡（现在只在时间线里有一条）
- **联系方式**：页脚写的是「邮箱与电话 · 见简历」，没有放真实手机号（公开网页放手机号容易被爬）。
  要放邮箱的话，告诉我。
